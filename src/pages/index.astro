---
import Layout from "../layouts/Layout.astro";
import { getGalleryImages } from '../utils/gallery';

const galleryImages = getGalleryImages();

function getLayoutClass(index) {
  if (index === 0) return 'col-span-2 row-span-2';
  if (index >= 1 && index <= 4) return 'col-span-1 row-span-1';
  return 'col-span-1 row-span-1';
}
---

<Layout>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="SANDWEDGES - Your favourite landmark cafe. Pure veg sandwiches, shakes & fries. Visit us today.">
  <title>SANDWEDGES | Your Favourite Landmark Cafe</title>
  <link rel="icon" href="/logo.jpg" sizes="32x32" type="image/jpeg">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
  
  <style>
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      scroll-behavior: smooth;
    }

    /* Desktop header hide on scroll */
    .header-hidden {
      transform: translateX(-50%) translateY(-140%);
    }

    /* Mobile sidebar animations */
    .sidebar-overlay {
      transition: opacity 0.3s ease-in-out;
    }
    
    .sidebar-overlay.hidden {
      opacity: 0;
      pointer-events: none;
    }

    .sidebar-panel {
      transition: transform 0.3s ease-in-out;
    }

    .sidebar-panel.closed {
      transform: translateX(100%);
    }

    /* Prevent body scroll when sidebar is open */
    body.sidebar-open {
      overflow: hidden;
    }
  </style>
</head>

<body class="bg-stone-50 text-zinc-900 antialiased">
  
  <!-- Header - Desktop & Mobile -->
  <header 
    id="site-header"
    class="fixed top-4 left-1/2 -translate-x-1/2 z-50 w-full max-w-6xl px-4 transition-transform duration-300 ease-in-out"
  >
    <div class="bg-white border border-zinc-200 rounded-full shadow-sm px-4 sm:px-6 py-3 flex items-center justify-between">
      <!-- Brand -->
      <a href="/" class="font-black text-lg sm:text-xl tracking-tight hover:opacity-80 transition-opacity" style="color: #F7931E;">SANDWEDGES</a>
      
      <!-- Desktop Navigation (hidden on mobile) -->
      <nav class="hidden md:flex items-center gap-8 text-sm font-medium" style="color: #F7931E;">
        <a href="#about" class="hover:text-orange-600 transition-colors">About</a>
        <a href="#menu" class="hover:text-orange-600 transition-colors">Menu</a>
        <a href="#gallery" class="hover:text-orange-600 transition-colors">Gallery</a>
        <a href="#contact" class="hover:text-orange-600 transition-colors">Location</a>
      </nav>
      
      <!-- Desktop CTA (hidden on mobile) -->
      <a href="https://www.instagram.com/sandwedges/" 
         class="hidden md:block hover:bg-orange-500 text-white px-6 py-2 rounded-full text-sm font-semibold transition-colors" 
         style="background-color: #F7931E;">
        Social
      </a>

      <!-- Mobile Hamburger Menu (visible only on mobile) -->
      <button 
        id="mobile-menu-btn"
        class="md:hidden w-10 h-10 flex items-center justify-center rounded-full hover:bg-orange-50 transition-colors"
        aria-label="Open menu"
      >
        <svg class="w-6 h-6" style="color: #F7931E;" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
        </svg>
      </button>
    </div>
  </header>

  <!-- Mobile Dropdown Menu -->
  <div 
    id="mobile-dropdown"
    class="fixed top-[88px] left-4 right-4 z-40 md:hidden bg-white border border-zinc-200 rounded-2xl shadow-xl hidden"
  >
    <nav class="flex flex-col divide-y divide-zinc-200">
      <a href="#about" class="mobile-link px-6 py-4 text-base font-semibold text-zinc-900 hover:bg-orange-50">
        About
      </a>
      <a href="#menu" class="mobile-link px-6 py-4 text-base font-semibold text-zinc-900 hover:bg-orange-50">
        Menu
      </a>
      <a href="#gallery" class="mobile-link px-6 py-4 text-base font-semibold text-zinc-900 hover:bg-orange-50">
        Gallery
      </a>
      <a href="#contact" class="mobile-link px-6 py-4 text-base font-semibold text-zinc-900 hover:bg-orange-50">
        Location
      </a>

      <div class="p-4">
        <a 
          href="https://www.instagram.com/sandwedges/" 
          class="block w-full py-3 text-center text-white font-bold rounded-full bg-orange-500 hover:bg-orange-600 transition-colors"
        >
          Follow on Instagram
        </a>
      </div>
    </nav>
  </div>

  <!-- Hero Section - Fully Responsive -->
  <section class="relative min-h-screen flex items-center pt-24 sm:pt-32 pb-16 sm:pb-20 px-4 sm:px-6 bg-orange-500">
    <div class="max-w-7xl mx-auto w-full grid md:grid-cols-2 gap-8 md:gap-12 items-center">
      <!-- Left: Text Content -->
      <div class="text-center md:text-left">
        <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-black tracking-tighter mb-4 sm:mb-6 text-white">
          SANDWEDGES
        </h1>
        
        <p class="text-2xl sm:text-3xl md:text-4xl font-bold text-white mb-4 sm:mb-6">
          Your Favourite Landmark Cafe
        </p>
        
        <p class="text-base sm:text-lg text-orange-100 leading-relaxed mb-8 sm:mb-10 max-w-lg mx-auto md:mx-0">
          100% Pure Veg. No delivery. No shortcuts. Just fresh sandwiches, crispy fries, and thick shakes worth walking to.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center md:justify-start">
          <a href="https://maps.app.goo.gl/nucb918UUVvwgePFA" 
             class="px-8 sm:px-10 py-3 sm:py-4 bg-white hover:bg-orange-100 text-orange-500 font-bold rounded-full transition-all duration-200 hover:scale-105 shadow-lg text-center text-sm sm:text-base">
            Get Directions
          </a>
          
          <a href="#menu" 
             class="px-8 sm:px-10 py-3 sm:py-4 bg-transparent border-2 border-white hover:bg-white text-white hover:text-orange-500 font-bold rounded-full transition-all duration-200 text-center text-sm sm:text-base">
            View Menu
          </a>
        </div>
      </div>

      <!-- Right: Image - Visible on all screens -->
      <div class="block md:block">
        <img 
          src="/hero.png" 
          alt="SANDWEDGES cafe" 
          class="w-full h-auto max-h-[550px] object-cover rounded-3xl shadow-xl shadow-orange-500/50"
        />
      </div>
    </div>
  </section>

  <!-- About Section - Responsive -->
  <section id="about" class="py-12 sm:py-16 md:py-20 px-4 sm:px-6 bg-white">
    <div class="max-w-3xl mx-auto text-center">
      <div class="inline-block mb-3 sm:mb-4">
        <span class="text-xs uppercase tracking-widest text-orange-500 font-bold">About Us</span>
      </div>
      <h2 class="text-3xl sm:text-4xl md:text-5xl font-black text-zinc-900 mb-6 sm:mb-8 px-4">
        A Neighbourhood Landmark
      </h2>
      <div class="w-16 h-1 bg-orange-500 mx-auto mb-6 sm:mb-8"></div>
      
      <p class="text-base sm:text-lg text-zinc-600 leading-relaxed mb-4 sm:mb-6 px-4">
        People don't order SANDWEDGES — they come to it. We're the cafe you walk to, not scroll past. Fresh veg ingredients, comfort food done right, and a vibe that keeps you coming back.
      </p>
      
      <p class="text-base sm:text-lg text-zinc-600 leading-relaxed mb-8 px-4">
        Coffee culture meets street food energy. This is your go-to spot.
      </p>

      <!-- Features Grid - Responsive -->
      <div class="grid sm:grid-cols-2 md:grid-cols-3 gap-6 sm:gap-8 mt-10 sm:mt-12 px-4">
        <div class="text-center">
          <div class="w-14 h-14 sm:w-16 sm:h-16 mx-auto mb-3 sm:mb-4 rounded-full bg-orange-500 flex items-center justify-center">
            <span class="text-xl sm:text-2xl">🥪</span>
          </div>
          <h3 class="text-base sm:text-lg font-black text-zinc-900 mb-2">Crafted Daily</h3>
          <p class="text-sm text-zinc-600 px-2">
            Every sandwich is made fresh when you order. No pre-made stuff, no shortcuts. Just real ingredients, real flavors.
          </p>
        </div>

        <div class="text-center">
          <div class="w-14 h-14 sm:w-16 sm:h-16 mx-auto mb-3 sm:mb-4 rounded-full bg-orange-500 flex items-center justify-center">
            <span class="text-xl sm:text-2xl">🌱</span>
          </div>
          <h3 class="text-base sm:text-lg font-black text-zinc-900 mb-2">100% Vegetarian</h3>
          <p class="text-sm text-zinc-600 px-2">
            Pure veg kitchen, zero compromise. We source the freshest vegetables and create magic with plant-based ingredients.
          </p>
        </div>

        <div class="text-center sm:col-span-2 md:col-span-1">
          <div class="w-14 h-14 sm:w-16 sm:h-16 mx-auto mb-3 sm:mb-4 rounded-full bg-orange-500 flex items-center justify-center">
            <span class="text-xl sm:text-2xl">🏆</span>
          </div>
          <h3 class="text-base sm:text-lg font-black text-zinc-900 mb-2">Community First</h3>
          <p class="text-sm text-zinc-600 px-2">
            More than a cafe, we're your daily ritual. The place where friendships happen, ideas spark, and memories are made.
          </p>
        </div>
      </div>

      <!-- Story Box - Responsive -->
      <div class="mt-10 sm:mt-12 p-6 sm:p-8 bg-orange-500 rounded-2xl mx-4">
        <h3 class="text-xl sm:text-2xl font-black text-white mb-3 sm:mb-4">Our Story</h3>
        <p class="text-sm sm:text-base text-white leading-relaxed mb-3 sm:mb-4">
          Started in 2020 with a simple mission: serve honest, delicious food that makes people happy. What began as a small sandwich counter has grown into the neighbourhood's most loved hangout spot.
        </p>
        <p class="text-sm sm:text-base text-white leading-relaxed">
          We believe great food doesn't need to be complicated. It needs to be fresh, made with care, and served with a smile. That's the SANDWEDGES promise.
        </p>
      </div>
    </div>
  </section>

  <!-- Menu Highlights - Responsive -->
  <section id="menu" class="py-12 sm:py-16 md:py-20 px-4 sm:px-6 bg-orange-500">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-10 sm:mb-16">
        <div class="inline-block mb-3 sm:mb-4">
          <span class="text-xs uppercase tracking-widest text-white font-bold">Pure Veg Menu</span>
        </div>
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-black text-white mb-4 sm:mb-6 px-4">
          Menu Highlights
        </h2>
        <div class="w-16 h-1 bg-white mx-auto"></div>
      </div>
      
      <!-- Menu Image - Fully responsive with max constraints -->
      <div class="flex justify-center px-4">
        <img 
          src="/menu.png" 
          alt="SANDWEDGES Menu" 
          class="w-full max-w-4xl h-auto rounded-xl sm:rounded-2xl shadow-lg"
        />
      </div>
    </div>
  </section>

    <!-- Gallery Section -->
  <section id="gallery" class="py-16 sm:py-20 px-4 sm:px-6 bg-white">
    <div class="max-w-7xl mx-auto">
      <!-- Section Header -->
      <div class="text-center mb-12">
        <p class="text-sm sm:text-base font-medium text-zinc-500 mb-2 uppercase tracking-wider">Gallery</p>
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-black text-zinc-900 mb-4">Inside SANDWEDGES</h2>
        <p class="text-lg text-zinc-600 max-w-2xl mx-auto">A glimpse into our space, our food, and the good vibes that make us special.</p>
      </div>

      <!-- Responsive Gallery Collage -->
      <div class="relative">
        <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 auto-rows-[200px] sm:auto-rows-[250px] lg:auto-rows-[300px] gap-3 sm:gap-4 lg:gap-6">
          {galleryImages.map((img, index) => (
            <div
              class={`${getLayoutClass(index)} group relative overflow-hidden rounded-2xl sm:rounded-3xl shadow-lg hover:shadow-2xl transition-all duration-500 hover:scale-[1.02]`}
            >
              <img
                src={`/gallery/${img}`}
                alt={`Gallery image ${index + 1}`}
                class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
                loading={index < 4 ? "eager" : "lazy"}
              />

              <div class="absolute inset-0 bg-gradient-to-t from-black/20 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>

              <div class="absolute top-3 right-3 bg-white/90 backdrop-blur-sm text-zinc-800 text-xs font-semibold px-2 py-1 rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                {index + 1}
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Gallery Stats -->
      <div class="mt-12 text-center">
        <div class="inline-flex items-center gap-6 sm:gap-8 text-sm sm:text-base text-zinc-600">
          <div class="flex items-center gap-2">
           
          <div class="flex items-center gap-2">
            <div class="w-2 h-2 bg-orange-500 rounded-full"></div>
            <span>Daily Updates</span>
          </div>
          
          <div class="flex items-center gap-2">
            <div class="w-2 h-2 bg-orange-500 rounded-full"></div>
            <span>100% Real</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Why SANDWEDGES - Responsive -->
  <section class="py-12 sm:py-16 md:py-20 px-4 sm:px-6 bg-white">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-10 sm:mb-16">
        <div class="inline-block mb-3 sm:mb-4">
          <span class="text-xs uppercase tracking-widest text-orange-500 font-bold">Why Us</span>
        </div>
        <h2 class="text-3xl sm:text-4xl md:text-5xl font-black text-zinc-900 mb-4 sm:mb-6 px-4">
          Why SANDWEDGES
        </h2>
        <div class="w-16 h-1 bg-orange-500 mx-auto"></div>
      </div>

      <!-- Reasons Grid - Responsive -->
      <div class="grid sm:grid-cols-2 md:grid-cols-3 gap-8 sm:gap-10 px-4">
        <!-- Point 1 -->
        <div class="text-center">
          <div class="w-16 h-16 sm:w-20 sm:h-20 mx-auto mb-4 sm:mb-6 rounded-full bg-orange-100 flex items-center justify-center">
            <svg class="w-8 h-8 sm:w-10 sm:h-10 text-orange-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z"/>
            </svg>
          </div>
          <h3 class="text-lg sm:text-xl font-black text-zinc-900 mb-3 sm:mb-4">
            100% Pure Veg
          </h3>
          <p class="text-sm sm:text-base text-zinc-600 leading-relaxed px-2">
            Every item on our menu is pure vegetarian. Fresh, flavourful, and guilt-free.
          </p>
        </div>

        <!-- Point 2 -->
        <div class="text-center">
          <div class="w-16 h-16 sm:w-20 sm:h-20 mx-auto mb-4 sm:mb-6 rounded-full bg-orange-100 flex items-center justify-center">
            <svg class="w-8 h-8 sm:w-10 sm:h-10 text-orange-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
            </svg>
          </div>
          <h3 class="text-lg sm:text-xl font-black text-zinc-900 mb-3 sm:mb-4">
            Fresh & Fast
          </h3>
          <p class="text-sm sm:text-base text-zinc-600 leading-relaxed px-2">
            Made to order, served hot. No waiting around, no compromises on quality.
          </p>
        </div>

        <!-- Point 3 -->
        <div class="text-center sm:col-span-2 md:col-span-1">
          <div class="w-16 h-16 sm:w-20 sm:h-20 mx-auto mb-4 sm:mb-6 rounded-full bg-orange-100 flex items-center justify-center">
            <svg class="w-8 h-8 sm:w-10 sm:h-10 text-orange-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
            </svg>
          </div>
          <h3 class="text-lg sm:text-xl font-black text-zinc-900 mb-3 sm:mb-4">
            Your Go-To Cafe Spot
          </h3>
          <p class="text-sm sm:text-base text-zinc-600 leading-relaxed px-2">
            A place you'll keep coming back to. Good food, good vibes, every single time.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Visit CTA Section - Responsive -->
  <section id="visit" class="py-12 sm:py-16 md:py-20 px-4 sm:px-6 bg-orange-500">
    <div class="max-w-4xl mx-auto text-center">
      <h2 class="text-3xl sm:text-4xl md:text-5xl font-black text-white mb-4 sm:mb-6 px-4">
        This One's Worth Visiting.
      </h2>
      <p class="text-lg sm:text-xl text-orange-100 mb-8 sm:mb-10 max-w-2xl mx-auto font-medium px-4">
        No delivery. No shortcuts. Just good food and good vibes.
      </p>
      
      <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center items-center px-4">
        <a href="https://maps.app.goo.gl/nucb918UUVvwgePFA" 
           class="w-full sm:w-auto px-10 sm:px-12 py-4 sm:py-5 bg-white hover:bg-zinc-100 text-orange-500 text-base sm:text-lg font-black rounded-full transition-all duration-200 hover:scale-105 shadow-xl text-center">
          Get Directions
        </a>
        
        <a href="https://www.instagram.com/sandwedges/" 
           class="w-full sm:w-auto px-10 sm:px-12 py-4 sm:py-5 bg-transparent border-2 border-white hover:bg-white text-white hover:text-orange-500 text-base sm:text-lg font-black rounded-full transition-all duration-200 text-center">
          Social
        </a>
      </div>
    </div>
  </section>

  <!-- Footer - Responsive -->
  <footer id="contact" class="py-10 sm:py-12 px-4 sm:px-6 bg-zinc-900 text-white">
    <div class="max-w-6xl mx-auto">
      <div class="grid sm:grid-cols-2 md:grid-cols-3 gap-8 sm:gap-10 mb-8 sm:mb-10">
        <!-- Brand -->
        <div class="text-center sm:text-left">
          <h3 class="text-xl sm:text-2xl font-black mb-2 sm:mb-3" style="color: #F7931E;">SANDWEDGES</h3>
          <p class="text-zinc-400 text-sm">
            Your Favourite Landmark Cafe
          </p>
        </div>

        <!-- Contact Info -->
        <div class="text-center sm:text-left">
          <h4 class="text-white font-bold mb-3 uppercase tracking-wider text-sm">Visit Us</h4>
          <div class="text-zinc-400 space-y-2 text-sm">
            <p>C-74, opposite petrol pump</p>
            <p>RDC, Sector 15, Sector 10, Raj Nagar</p>
            <p>Ghaziabad, Uttar Pradesh 201002</p>
            <p class="pt-2">
              <a href="tel:+911234567890" class="hover:text-orange-500 transition-colors">
                +91 123 456 7890
              </a>
            </p>
          </div>
        </div>

        <!-- Hours -->
        <div class="text-center sm:text-left sm:col-span-2 md:col-span-1">
          <h4 class="text-white font-bold mb-3 uppercase tracking-wider text-sm">Hours</h4>
          <div class="text-zinc-400 space-y-2 text-sm">
            <p>Monday - Friday: 9am - 10pm</p>
            <p>Saturday - Sunday: 10am - 11pm</p>
            <p class="pt-3 font-semibold text-orange-500">
              @sandwedges
            </p>
          </div>
        </div>
      </div>

      <!-- Bottom Bar -->
      <div class="pt-6 sm:pt-8 border-t border-zinc-800 flex flex-col md:flex-row justify-between items-center gap-4">
        <p class="text-zinc-500 text-xs sm:text-sm text-center md:text-left">
          © 2026 SANDWEDGES. All rights reserved.
        </p>
      </div>
    </div>
  </footer>

  <!-- Sticky Mobile CTA - Bottom padding adjusted to prevent content blocking -->
  <div class="fixed bottom-4 left-4 right-4 md:hidden z-40">
    <a href="https://maps.app.goo.gl/nucb918UUVvwgePFA" 
       class="block w-full py-4 bg-white text-orange-500 text-center font-black rounded-full shadow-2xl hover:bg-orange-50 transition-colors text-sm border-2 border-orange-500">
      Get Directions
    </a>
  </div>

  <script>
    // Desktop header hide on scroll
    let lastScrollY = window.scrollY;
    const header = document.getElementById("site-header");

    window.addEventListener("scroll", () => {
      const currentScrollY = window.scrollY;

      if (currentScrollY > lastScrollY && currentScrollY > 80) {
        header.classList.add("header-hidden");
      } else {
        header.classList.remove("header-hidden");
      }

      lastScrollY = currentScrollY;
    });

    // Mobile dropdown menu functionality
    const menuBtn = document.getElementById("mobile-menu-btn");
    const dropdown = document.getElementById("mobile-dropdown");
    const mobileLinks = document.querySelectorAll(".mobile-link");

    function toggleDropdown() {
      dropdown.classList.toggle("hidden");
    }

    function closeDropdown() {
      dropdown.classList.add("hidden");
    }

    menuBtn.addEventListener("click", (e) => {
      e.stopPropagation();
      toggleDropdown();
    });

    mobileLinks.forEach(link => {
      link.addEventListener("click", closeDropdown);
    });

    // Close when clicking outside
    document.addEventListener("click", (e) => {
      if (!dropdown.contains(e.target) && !menuBtn.contains(e.target)) {
        closeDropdown();
      }
    });

    // Close on scroll
    window.addEventListener("scroll", closeDropdown);

    // Smooth scrolling for all anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });

    </script>

</body>
</html>
</Layout>